In EncryptBC, the arguments should be ordered like this '-i example.txt'.

In HashBC, the arguments should be ordered like this 'md5 -i example.txt'. The two hash name arguments are 'md5' and 'sha256',